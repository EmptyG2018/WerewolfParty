# WerewolfParty 项目上下文

本文档记录当前狼人杀项目的规则、流程和实现边界，用于后续开发时保持一致。若代码与本文档不一致，先核对 `packages/shared`、`packages/server/src/game`、`packages/server/src/rooms` 的当前实现，再同步更新本文档。

## 项目结构

- 共享模型与规则定义在 `packages/shared`：
  - `roles.ts` 定义身份、阵营、技能能力、预设板子。
  - `room.ts` 定义房间配置、配置校验、阵营人数统计。
  - `game.ts` 定义阶段、玩家状态、死亡原因、发言状态、投票历史。
- 服务端核心流程在 `packages/server/src/game`：
  - `GameEngine.ts` 负责纯逻辑：发牌、夜晚结算、投票结算、胜负判定、发言顺序。
  - `GameManager.ts` 负责 Socket 事件、阶段推进、计时器、私有信息推送、死亡技能流程。
- 房间、重连、座位交换在 `packages/server/src/rooms/RoomManager.ts`。

## 游戏配置规则

- 玩家人数范围：`7-12`。
- 默认配置：
  - `maxPlayers = 9`
  - `roles = [werewolf, seer, witch, hunter]`
  - `wolfCount = 3`
  - `voteTime = 60`
  - `roleConfirmTime = 30`
  - `allowWitchSelfSave = false`
  - `allowWolfFriendlyFire = true`
  - `hybridRoles = []`
- 村民由系统自动补齐，不允许在 `roles` 中手动选择 `villager`。
- `roles` 必须包含 `werewolf`，且至少有 1 名狼人。
- 特殊角色不能重复选择。
- 显式配置角色数不能超过总人数，且自动补齐后至少有 1 名村民。
- 狼人阵营总数必须小于总人数的一半。
- 神民同体只允许应用于已启用的非狼人神职角色。
- 当前预设：
  - 9 人标准局：3 狼、预言家、女巫、猎人，其余村民。
  - 12 人进阶局：3 普通狼、白狼王、预言家、女巫、猎人、守卫，其余村民。

## 房间与座位规则

- 房间状态：`waiting`、`playing`、`finished`。
- 只有房主可以修改配置、开始游戏、暂停/恢复游戏、结束后重新开局。
- 开局条件：
  - 房间人数必须等于 `config.maxPlayers`。
  - 所有玩家必须在线。
  - 所有玩家必须已准备，房主也不例外。
  - 房间必须处于等待状态。
- 新进入等待房间的玩家默认未准备；调试补齐玩家可自动准备，避免阻塞调试流程。
- 房主修改游戏配置后，所有玩家准备状态重置为未准备，避免规则变化后沿用旧确认。
- 结束后重新开局会重置所有玩家准备状态，必须重新准备后才能开始下一局。
- 玩家稳定身份使用 `sessionId/player.id`，Socket 断线重连后会变化，不能把 `socket.id` 当成持久玩家身份。
- UI 与局内称呼中的“几号玩家”按座位号展示，即 `seatIndex + 1`。
- `playerNumber` 是历史兼容字段；新增局内显示、日志、投票分析应优先使用座位号，不应把 `playerNumber` 当成当前显示语义。
- 座位交换只允许在等待房间内进行。
- 与空座交换时直接移动座位。
- 与有人的座位交换时需要目标玩家同意。
- 一个玩家同一时间只能参与一个待处理交换；不相关的两组玩家可以同时交换，例如 1 与 2 交换时，3 与 4 仍可交换。
- 交换成功只交换玩家所在座位，不交换玩家身份、会话或角色。
- 发起者可以取消交换；目标玩家拒绝、离开或发起者离开时，要通知对方交换取消。

## 核心阶段流程

标准主流程：

1. `waiting`：等待房间满员、配置完成、所有玩家在线并准备。
2. `role_confirm`：发牌后每个玩家确认身份；全部确认或倒计时结束后进入夜晚。
3. 夜晚子阶段按配置运行：
   - `night_werewolf`
   - `night_seer`，仅配置了具备查验能力的角色时存在。
   - `night_guard`，仅配置了守卫能力角色时存在。
   - `night_witch`，仅配置了女巫救/毒能力角色时存在。
4. `day_announce`：公布昨夜死亡或平安夜。
5. `day_speaking`：存活玩家按服务端生成顺序依次发言。
6. `day_vote`：存活玩家投票或弃票。
7. 若公投出局，进入 `last_words`；否则直接做死亡后检查。
8. 死亡后检查胜负；未结束则天数加一并进入下一夜。
9. 胜负成立时进入 `game_over`。

特殊打断流程：

- 狼人/狼王白天自曝：进入 `day_self_reveal` 短暂停留，然后直接进入下一夜。
- 白狼王投票阶段自曝带人：自曝者与目标死亡，短暂停留后进入下一夜。
- 猎人可开枪时：临时进入 `hunter_shoot`，结束后恢复原流程。
- 狼王被狼人夜刀死亡时：天亮公告前进入 `wolf_king_shoot`，结束后进入白天公告。

## 计时与阶段推进

- 阶段倒计时以服务端 `phaseEndsAt` 为准，客户端只做展示和校准。
- 阶段推进期间使用短暂过渡延迟，避免 UI 无感知瞬间跳阶段。
- `phaseAdvancingRooms` 用于阻止阶段推进窗口内重复提交动作。
- 阶段暂停时保留剩余时间和原阶段回调；恢复后继续原流程。
- 夜晚子阶段若对应存活执行者不存在，会自动跳过，避免流程卡死。
- 自动跳过已死亡角色阶段时，UI 不应用异常提示或明显瞬时行为暴露该身份是否已淘汰。

## 身份与阵营规则

- 当前身份：
  - 村民：`villager`
  - 狼人：`werewolf`
  - 狼王：`wolf_king`
  - 白狼王：`white_wolf_king`
  - 预言家：`seer`
  - 女巫：`witch`
  - 猎人：`hunter`
  - 守卫：`guard`
- 阵营/分组：
  - 狼人组：普通狼人、狼王、白狼王。
  - 神职组：预言家、女巫、猎人、守卫。
  - 村民组：村民。
- 神民同体只改变胜负判定中的分组统计，不改变技能、不改变预言家查验结果。
- 预言家查验是否为狼人按基础身份判断，神民同体不会被查成狼人。

## 技能规则

### 狼人

- 狼人夜晚可以选择击杀目标并确认投票。
- 狼人点击目标会先形成“预选择”，预选择需要实时同步给狼队，但不算最终刀票。
- 狼人确认刀票必须二次确认；确认后本轮不能修改。
- 狼队内部预选择与确认状态只同步给狼队，不能发给非狼人。
- 是否允许狼人自刀或刀狼队友由房间配置 `allowWolfFriendlyFire` 控制，默认开启。
- 开启时主动刀人可以选择任意存活玩家，包括自己和狼队友。
- 关闭时狼人只能选择存活非狼人；服务端必须拒绝狼队目标，客户端也不能将狼队目标作为可选目标。
- 所有存活狼人确认后，狼人阶段提前结算。
- 狼人阶段超时：
  - 未确认的存活狼人视为弃票。
  - 按已确认狼票结算。
  - 若没有任何已确认狼票，则随机刀一名存活非狼人，避免空刀。
- 狼队投票平票时，从最高票候选中随机选择目标。
- 全部未确认时不允许空刀，由系统随机刀存活非狼人；若后续支持显式狼队弃刀，需要同步调整规则。
- 狼刀最终结果记录为 `wolfKill`，不能与预言家查验、女巫毒药、守卫守护复用同一个 key。

### 预言家

- 每晚查验一名存活目标。
- 查验结果只发给预言家本人。
- 查验不会写入夜晚死亡行动，也不会影响夜晚死亡结算。

### 女巫

- 女巫有一瓶解药和一瓶毒药，各限本局使用一次。
- 解药只能在当晚存在狼刀目标时使用。
- 是否允许女巫自救由房间配置 `allowWitchSelfSave` 控制，默认关闭。
- 未开启 `allowWitchSelfSave` 时，女巫不能用解药救自己；开启后可救自己。
- 毒药目标必须是存活玩家，且不能毒自己。
- 女巫可以选择跳过。
- 解药通过当晚 `witchSavedTonight` 抵消狼刀；毒药写入 `witchPoison`。
- 同守同救规则：同一晚守卫守护了狼刀目标，女巫也对该狼刀目标使用解药时，狼刀不被抵消，该目标死亡。
- 女巫药品剩余状态只通过私有 `game:skillState` 发给女巫本人，用于跨夜与重连后的 UI 恢复。

### 守卫

- 守卫每晚可以守护一名存活玩家。
- 不能连续两晚守护同一目标。
- 守护写入 `guardProtect`，若目标等于狼刀目标且女巫没有同时使用解药，则抵消狼刀。
- 上一晚守护目标只通过私有 `game:skillState` 发给守卫本人，用于禁用连续守护同一目标并支持重连恢复。

### 猎人

- 猎人当前可开枪场景：
  - 被狼人夜里刀死：天亮公告后可开枪或不开枪。
  - 被白天公投出局：先遗言，再可开枪或不开枪。
- 猎人被女巫毒、被猎人枪、被狼王枪、被白狼王爆炸带走时，不触发开枪。
- 猎人开枪目标必须是存活玩家。
- 被猎人开枪带走的玩家死亡原因为 `shot`，没有遗言，也不触发新的死亡技能。
- 猎人每局每名玩家只可开枪一次；超时等同不开枪。

### 狼王

- 狼王参与狼队夜晚击杀。
- 狼王被狼人夜刀死亡时可在天亮公告前开枪。
- 狼王被毒、被投、被技能带走时，当前实现不触发狼王开枪。
- 狼王开枪死亡原因为 `shot`，不触发目标遗言或二次死亡技能。
- 狼王白天可以自曝中断白天流程。

### 白狼王

- 白狼王参与狼队夜晚击杀。
- 白狼王白天可以普通自曝不带人，规则同狼人自曝：自己出局，中断当天白天流程并进入下一夜。
- 白狼王发言阶段不能自曝带人。
- 白狼王投票阶段可以自曝并带走一名存活目标。
- 自曝者死亡原因为 `self_exposed`，被带走目标死亡原因为 `exploded`。
- 白狼王自曝带人会中断当天白天流程，短暂停留后进入下一夜。
- 被爆炸带走的玩家没有遗言，也不触发死亡技能。

## 夜晚结算规则

- 夜晚行动使用独立 key：
  - `wolfKill`
  - `witchPoison`
  - `guardProtect`
- 结算顺序：
  1. 读取狼刀目标。
  2. 若守卫守护目标等于狼刀目标，且女巫没有同时救该目标，抵消狼刀。
  3. 若女巫当晚使用解药，且守卫没有同时守护狼刀目标，抵消狼刀。
  4. 若守卫和女巫解药同时作用于狼刀目标，触发同守同救，狼刀目标死亡。
  5. 合并女巫毒药死亡。
  6. 用集合去重，避免同一玩家重复死亡记录。
- 狼刀死亡原因：`killed`。
- 女巫毒药死亡原因：`poisoned`。
- 如果狼刀目标是狼王，设置 `wolfKingCanShoot = true` 并进入狼王开枪阶段。
- 夜晚结算后先判断胜负；若未结束，再进入天亮公告或死亡技能流程。
- 夜晚死亡可以在服务端内部立即结算，用于胜负和死亡技能判断；但公开 `game:playerDead` 事件必须延迟到 `day_announce`，不能在狼王/猎人隐藏结算窗口提前广播夜晚死亡名单。

## 发言规则

- 白天发言只包含存活玩家。
- 发言顺序由服务端生成：
  - 按座位号排序。
  - 若昨夜有狼刀目标，从该座位之后的存活玩家开始。
  - 正序或反序随机。
- 每名发言者有固定倒计时。
- 当前发言者点击完成可提前进入下一位。
- 超时自动进入下一位。
- 全部发言完成后进入投票阶段。
- 遗言复用发言状态结构，但只包含出局玩家本人。

## 投票规则

- 只有存活玩家可在 `day_vote` 投票。
- 不能投自己。
- 可以显式弃票。
- 倒计时结束时，所有未投票的存活玩家补记为 `null`，等同弃票。
- 所有存活玩家都已投票或弃票时，提前结束等待并结算，不必等倒计时归零。
- 投票结果：
  - 最高票唯一时，该目标被公投出局。
  - 平票时无人出局。
  - 全部弃票时无人出局。
- 投票历史只记录已经完成的投票轮次，不做实时投票明细同步。
- 投票历史按天保存；同一天重复结算时覆盖同一天记录，避免出现重复“第 x 天投票”。
- 投票历史需要包含：
  - 每个存活玩家的投票目标或 `null` 弃票。
  - 每个目标的得票数。
  - 弃票人数。
  - 是否平票。
  - 出局玩家，如果无人出局则为 `null`。

## 遗言规则

- 只有白天公投出局，即死亡原因 `voted`，触发遗言。
- 以下死亡不触发遗言：
  - 狼人夜刀：`killed`
  - 女巫毒药：`poisoned`
  - 猎人或狼王开枪：`shot`
  - 狼人/狼王/白狼王自曝：`self_exposed`
  - 白狼王爆炸带走：`exploded`
- 被公投出局的猎人流程是：先遗言，再选择开枪或不开枪。
- 被枪、被毒、被爆炸带走的玩家没有遗言。

## 死亡原因与后果

- `killed`：狼人夜刀死亡。
  - 可触发猎人天亮后开枪。
  - 若死者是狼王，可触发狼王开枪。
  - 不触发遗言。
- `voted`：白天公投出局。
  - 触发遗言。
  - 若死者是猎人，遗言后可开枪。
- `poisoned`：女巫毒药死亡。
  - 不触发遗言。
  - 不触发猎人/狼王死亡技能。
- `shot`：猎人或狼王开枪死亡。
  - 不触发遗言。
  - 不触发二次死亡技能。
- `self_exposed`：狼人、狼王或白狼王自曝死亡。
  - 中断白天流程。
  - 不触发遗言。
- `exploded`：白狼王自曝带走目标死亡。
  - 不触发遗言。
  - 不触发二次死亡技能。

## 胜负判定

- 使用屠边规则。
- 存活狼人数量为 0：好人阵营胜利。
- 存活神职数量为 0：狼人阵营胜利。
- 存活村民数量为 0：狼人阵营胜利。
- 神民同体角色在胜负判定中同时计入神职组和村民组。
- 胜负判定在关键死亡结算后执行：
  - 夜晚死亡结算后。
  - 投票出局/遗言/猎人技能流程后。
  - 猎人或狼王开枪后。
  - 自曝或爆炸带人后。

## 信息隔离规则

- 每名玩家开局只收到自己的身份。
- 普通房间事件只发送 `PublicRoom/PublicPlayer`，其中 `players[].role` 必须为 `null`；所有玩家身份只能通过本人私有 `myRole` 或游戏结束 `game:over` 揭示。
- 狼队列表只发送给狼人阵营玩家。
- 狼人夜晚选择和确认投票状态只发送给狼队。
- 预言家查验结果只发送给预言家本人。
- 女巫阶段的狼刀目标只发送给存活女巫本人，用于显示“今晚被袭击”的玩家和解药操作。
- 女巫药品可用状态、守卫上晚守护目标只通过私有 `game:skillState` 发给对应玩家本人，不得广播给房间其他玩家。
- 服务端内部死亡原因 `DeathReason` 可保留真实值用于结算；发送给客户端的死亡事件和重连游戏状态必须使用公开死亡原因 `PublicDeathReason`。
- 狼刀 `killed` 与女巫毒药 `poisoned` 对客户端统一脱敏为 `night`，UI 只能显示“夜间死亡”，不能显示“狼人袭击”或“女巫毒杀”。
- 夜晚死亡名单在天亮公告前不得通过 `game:playerDead`、重连 `PublicGameState.deadPlayers` 或系统消息提前公开；隐藏死亡技能窗口内非行动者只能看到安全的结算等待。
- 猎人/狼王开枪是服务端隐藏阶段，只向行动者本人发送 `hunterRequired` / `wolfKingRequired`；房间其他玩家不能收到 `HUNTER_SHOOT` / `WOLF_KING_SHOOT` 的公开 `phaseChanged`。
- 非行动者在隐藏阶段开始、重连或暂停恢复时，只能看到安全的公共 `day_resolving` 结算等待视图，不能通过阶段名推断猎人或狼王身份。
- `PublicGameState` 不得包含服务端内部结算字段，包括 `nightActions`、`seerCheckResult`、`skillState`、女巫用药消耗标记、`lastKilledPlayer`、`lastGuardTarget`、`wolfKingCanShoot`、`wolfVotes`。
- 公开游戏状态中的实时 `votes` 只允许包含当前接收者本人的投票记录；完整投票明细只能在投票结算后进入 `voteHistory` 或 `game:voteResult`。
- 调试接口和客户端调试面板会暴露全身份视角，只能在显式启用 `ENABLE_DEBUG_TOOLS=true` 和 `VITE_ENABLE_DEBUG_TOOLS=true` 时使用，不能随普通开发环境自动开启。
- 投票历史在投票完成后公开，用于复盘和局势分析；不应实时公开未完成投票明细。
- 重连时需要重放：
  - 房间状态。
  - 房间状态必须使用 `PublicRoom`，不能包含其他玩家身份。
  - 当前游戏状态。
  - 公开游戏状态中的历史死亡记录必须脱敏为 `PublicDeathReason`。
  - 玩家本人身份。
  - 狼队信息，仅狼人可见。
  - 当前阶段、剩余时间、发言状态。
  - 已确认身份状态。
  - 狼队当前选择/确认状态，仅狼人可见。
  - 私有技能状态，仅对应女巫/守卫本人可见。
- 不应通过特殊倒计时、异常提示、跳阶段文案暴露某个身份是否已死亡或由机器人托管。

## UI 交互约束

- UI 显示“几号玩家”时使用座位号 `seatIndex + 1`。
- 等待大厅需要显示每名玩家准备状态，并提供本人“准备/取消准备”操作；房主开始按钮必须根据满员、在线、全员准备分别显示阻塞原因。
- 创建房间的规则选项需要展示 `allowWitchSelfSave`，默认不勾选；等待大厅也应展示当前是否允许女巫自救。
- 创建房间的规则选项需要展示 `allowWolfFriendlyFire`，默认勾选；等待大厅也应展示当前是否允许狼人自刀或刀狼队友。
- 座位交换应提供清晰状态：
  - 发起者看到等待对方确认，可取消。
  - 接收者看到谁想交换，可同意或拒绝。
  - 对方取消或离开时显示取消提示。
  - 目标玩家正在与别人交换时，发起者收到明确提示。
- 阶段切换应有可感知过渡，避免从预言家查验、女巫用药、投票结算等交互瞬间跳走。
- 投票记录建议以非阻塞弹窗或抽屉展示，减少主界面平铺占用。
- 夜晚角色行动 UI 必须按当前玩家身份和阶段显示，不能让无权限玩家看到私有目标、私有结果或狼队协商明细。
- 狼人刀人 UI 应支持点击玩家卡片实时预选择/切换，并在玩家卡片上轻量展示狼队待确认选择和已确认票数；最终确认必须走二次确认弹窗。
- 白狼王未发动前不要在公共 UI、阶段文案、倒计时上暴露特殊差异；自曝带人入口只对本人显示，按钮和死亡原因文案尽量使用“自曝/自曝带走/自曝出局”等中性表达。
- 女巫用药阶段需要显示私有狼刀目标；无狼刀目标时显示无人并禁用解药。
- 女巫和守卫 UI 应使用私有 `game:skillState` 恢复药品消耗与上晚守护目标；客户端默认值只能作为短暂等待态，服务端仍是最终校验来源。

## 开发注意事项

- 新增阶段、死亡原因或技能时，必须同时检查：
  - `GamePhase`
  - `DeathReason`
  - `GameState`
  - 服务器阶段计时与重连恢复
  - 客户端阶段 UI
  - 胜负判定触发点
  - 遗言和死亡技能是否触发
  - 信息隔离边界
- 不要把夜晚动作按角色名混用；会造成狼刀、查验、毒药等目标串线。
- 不要实时公开投票明细；只公开已结算的历史投票。
- 不要让技能击杀触发遗言或死亡技能链式结算，除非明确修改规则并同步所有入口。
- 不要用 `socket.id` 存储跨重连业务状态；使用 `player.id/sessionId`。
- 重连、暂停、阶段过渡、超时提交都要考虑幂等，避免重复结算。
- 修改座位语义时优先保护 `seatIndex` 作为显示号来源，避免回到稳定 `playerNumber` 称呼。
- 修改隐藏阶段、死亡原因、公开状态或信息隔离边界后，运行 `npm run test:regression`。该命令会构建项目并启动临时本地服务，覆盖猎人/狼王隐藏开枪、公开房间/游戏状态脱敏、预言家私有查验结果等 Socket 回归场景。
